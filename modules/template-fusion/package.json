{
  "name": "tu-cao-mvp1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:single": "vite build && node tools/build-single.mjs",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
